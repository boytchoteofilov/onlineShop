@model HomeViewModel
@{
    ViewData["Title"] = "Home Page";
}

@Html.Partial("Carousel")

<h2>Preferred drinks og the week</h2>

<div class="row">
    @foreach (var drink in Model.PreferredDrinks)
    {
       @Html.Partial("DrinkSummary", drink)
    }
</div>
